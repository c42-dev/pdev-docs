---
title: 'List Sub-accounts'
openapi: '/openapi.en.json GET /sub-accounts'
---

<Note>
Returns the sub-accounts of the main account, each with the `reference` you send in the `X-Sub-Account` header to operate on it. See [Sub-accounts](/en#sub-accounts) for the behavior of the header and the full error table.
</Note>
